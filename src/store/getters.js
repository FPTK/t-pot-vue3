export default {
    siderType: (state) => state.app.siderType,
    ThemeColor: (state) => state.app.ThemeColor,
    routers: (state) => state.permisson.routers,
    token:(state)=>state.permisson.token,
    status:(state)=>state.permisson.status,
    addRouters:(stata)=>stata.permisson.addRouters
}